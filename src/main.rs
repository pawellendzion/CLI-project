use rust_cli::{create_catalog, file_tree::node::Catalog, remove_entry};

fn main() -> Result<(), Box<dyn std::error::Error>>{
    // let catalog = Catalog::new(String::from("c1"));
    // create_catalog("/", catalog)?;

    // let catalog = Catalog::new(String::from("c1_1"));
    // create_catalog("/c1", catalog)?;

    // let catalog = Catalog::new(String::from("c1_2"));
    // create_catalog("/c1", catalog)?;

    // let catalog = Catalog::new(String::from("c2"));
    // create_catalog("/", catalog)?;

    // remove_entry("/c1")?;

    Ok(())
}
